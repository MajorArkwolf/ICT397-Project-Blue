#pragma once

	/// Internal Dependencies
#include "Entity.hpp"

	/*!
	 * @brief A static set of helper functions for bound systems incompatible with Entity's functions.
	 * @note This provides a set of template function implementations for the many GameObject types.
	 * @see Entity
	 * @see GameObject
	 */
namespace Entity_Helper {

};